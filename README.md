# DSSD

## Install
`npm i -g yarn`

### Frontend 
`yarn` : install dependencies
`yarn start` : will run on localhost:3001


### Backend 
`yarn` : install dependencies
`yarn start` : will run on localhost:3000
